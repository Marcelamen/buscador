const prompt = require("prompt-sync")(); 
var celsius, kelvin ;

	 celsius= parseFloat(prompt("Ingresa grados celsius"));
	kelvin = (celsius)+273.15;

	console.log(celsius+" centígrados equivalen a "+kelvin+" kevin ");